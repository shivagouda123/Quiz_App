# Quiz_App
This is a dynamic quiz application built using HTML, CSS, and JavaScript. The app allows users to answer multiple-choice questions and displays the final score at the end of the quiz. The application uses innerHTML for updating the UI dynamically and localStorage to save the user's score, enabling users to revisit the app and see their last score.
